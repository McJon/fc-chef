{% if section.settings.toggle %}
  <div class="message-bar">
    {% if section.settings.message_header != blank %}
      <h2>{{ section.settings.message_header }}</h2>
    {% endif %}

    <a href="{{ section.settings.announcement_page }}">
      {% unless section.settings.message_content == blank %}
        <p>{{ section.settings.message_content }}</p>
      {% endunless %}
    </a>

    <button>&#10062;</button>
  </div>
{% endif %}



{% schema %}
  {
    "name": "Announcement",
    "settings": [
      {
        "type": "page",
        "id": "announcement_page",
        "label": "Announcement"
      },
      {
        "type": "checkbox",
        "id": "toggle",
        "label": "Toggle Message Bar",
        "default": false
      },
      {
        "type": "text",
        "id": "message_header",
        "label": "Message Header"
      },
      {
        "type": "textarea",
        "id": "message_content",
        "label": "Message Content"
      }
    ]
  }
{% endschema %}
